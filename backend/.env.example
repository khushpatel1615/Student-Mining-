# ============================================
# StudentDataMining - Backend Configuration
# ============================================
# INSTRUCTIONS:
# 1. Copy this file to .env (DO NOT COMMIT .env to git!)
# 2. Fill in all values below
# 3. Generate secure secrets for production
# ============================================

# ============================================
# DATABASE CONFIGURATION
# ============================================
DB_HOST=localhost
DB_NAME=student_data_mining
DB_USER=root
DB_PASS=
# Leave DB_PASS empty if no password (XAMPP default)
# For production: Use a strong password and dedicated database user

# ============================================
# JWT AUTHENTICATION
# ============================================
# CRITICAL: Change JWT_SECRET to a random string (min 32 chars)
# Generate secure secret:
#   Linux/macOS: openssl rand -base64 32
#   Windows PS: [Convert]::ToBase64String((1..32 | %{Get-Random -Max 256}))
JWT_SECRET=CHANGE_THIS_TO_A_RANDOM_STRING_MINIMUM_32_CHARACTERS_LONG_FOR_SECURITY
JWT_EXPIRY=86400
# JWT_EXPIRY in seconds (86400 = 24 hours)

# ============================================
# GOOGLE OAUTH (Optional - for Google Sign-In)
# ============================================
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
# Leave blank to disable Google OAuth

# ============================================
# CORS CONFIGURATION
# ============================================
# Frontend URL for Cross-Origin Resource Sharing
ALLOWED_ORIGIN=http://localhost:5173
# For production, change to your actual domain:
# ALLOWED_ORIGIN=https://yourdomain.com

# ============================================
# AI CHAT CONFIGURATION (Optional)
# ============================================
# Google Gemini API Key for AI assistant feature
# Get key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
# Leave blank to disable AI Chat feature

# ============================================
# EMAIL NOTIFICATIONS (For At-Risk Student Alerts)
# ============================================
# SMTP Configuration for sending daily at-risk student emails to admins
# Leave blank to use PHP's built-in mail() function (less reliable)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password
SMTP_FROM=noreply@studentdatamining.edu
SMTP_FROM_NAME=Student Data Mining System
SMTP_SECURE=tls

# For Gmail: Enable 2FA and create an App Password
# https://myaccount.google.com/apppasswords

# ============================================
# SECURITY BEST PRACTICES
# ============================================
# ✅ NEVER commit .env to version control
# ✅ Use different secrets for dev/staging/production
# ✅ Rotate JWT_SECRET periodically (invalidates all tokens)
# ✅ Enable HTTPS in production
# ✅ Use strong, unique passwords
# ============================================
