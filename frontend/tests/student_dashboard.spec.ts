import { test, expect } from '@playwright/test';

test.describe('Student Dashboard', () => {
    test('Tabs load properly', async ({ page }) => {
        // Log in as student
        // Verify tabs: Overview, Grades, Attendance
        // Click each tab
    });
});
