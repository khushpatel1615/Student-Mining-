import { test, expect } from '@playwright/test';

test.describe('Admin Management', () => {
    test('Admin can perform CRUD on students', async ({ page }) => {
        // View students list
        // Click add student
        // Fill form, save
        // Search student
        // Delete student
    });
});
